/** Página: Time. Organização e voluntariado (features/team.js). */
function initTime() {
  initShell("time");
  renderTeamSection(TEAM, document.getElementById("teamSection"), document.querySelector(".team-grid"));
}

initTime();
